require "spec_helper"

describe WernerViewTool do
  it "has a version number" do
    expect(WernerViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
